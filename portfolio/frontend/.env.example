# The URL your Render backend is served from.
# For local dev against a local backend, use http://localhost:4000
# For production on Vercel, set this in the Vercel dashboard to
# something like https://portfolio-backend.onrender.com
VITE_API_URL=http://localhost:4000
